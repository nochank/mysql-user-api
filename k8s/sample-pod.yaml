apiVersion: v1
kind: Pod

metadata:
  name: sample-pod
  namespace: default

spec:
  containers:
    - name: sample-container
      image: curlimages/curl:latest

      command:
        - sleep
        - "3600"

  restartPolicy: Never